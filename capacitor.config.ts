import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '00000025471_Novia Rahman Nisa',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
